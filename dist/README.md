# Distribution files

`70 TV.drfx` is the ready-to-install package for DaVinci Resolve 21.

For normal use, download the same file from the latest GitHub Release. The copy
kept in this directory makes the repository reproducible and lets the validation
workflow compare the packaged title with `src/70 TV.setting`.
